/// A Calculator.
abstract class Calculator {
  static int sum(int x, int y) => x + y;
  static int subtraction(int x, int y) => x - y;
  static int product(int x, int y) => x * y;
  static double division(double x, double y) => x / y;
}
